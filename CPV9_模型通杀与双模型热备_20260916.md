# CPV9 模型通杀 + 双模型热备(Failover)落地记录

日期: 2026-09-16
分支: cpv9-cuda-main (feiyang2025)
状态: 代码完成并通过全部本地验证; 车上验证待用户实车回放

## 一、背景与目标

1. cp 通吃 sp 与 cp 的全部模型(Classic/CD210/FiletOFish/BigCombo/0-8 社区权重套件)
2. 移植官方(commaai/openpilot precompiled-big-model 分支)的"大模型主跑 + 小模型热备,
   大模型异常时小模型第一时间接管, 同一帧只发布一个源"机制
3. 确认 BigCombo(1.76GB supercombo)在 cp 上规划链路完整(不只是模型在推理)

## 二、关键事实(均已代码级核实)

- cp 的 ONNX 全部自带权重(CD210 vision 267 initializers/46MB, FiletOFish 205/34MB)
  → trtexec 可直接从 ONNX 重建 .plan,tinygrad.pkl 仅 tinygrad 回退路径使用
- cp/sp 的 parse_model_outputs.py 同款(plan/lead/desire_state MDN 全支持)
- 规划器链路: modeld(BigCombo POLICY_SLICES plan 917-1907)
  → fill_model_msg(填充 modelV2.position + action.desiredCurvature, plan 回退)
  → sp controlsd.py:134 吃 modelV2.action.desiredCurvature 做横向
  → plannerd.py LongitudinalPlanner+CarrotPlanner 消费 modelV2 做纵向
  → cp controlsd.py:201 同样吃 modelV2.action.desiredCurvature, :182 用 position.yStd
  结论: BigCombo 接入后规划链路自动闭合, 与 FiletOFish 同款
- cp 的 cereal ModelDataV2 无 `big` 字段(官方 chestnut 分支才有) → 用 Params FallbackActive 替代
- 重大发现: cp 的 modeld.py main() 原先直接 new tinygrad ModelState(cl_context),
  GpuModelState/_make_model 是无人调用的死代码(连 fallback 行参数都是错的,
  ModelState 只有 context 参数)——本次已接线并修复

## 三、改动清单

| 文件 | 改动 |
|---|---|
| selfdrive/modeld/modeld.py | ① main() 改用 _make_model(0,0,cl_context=...) 走 CUDA/TRT 主路径; ② _make_model 改引 FailoverModelState; ③ 修复 fallback 行 ModelState(cl_context) |
| openpilot/sunnypilot/modeld_v2/gpu_model_state.py | ① GpuModelState 加 model_name 参数; ② 新增 FailoverModelState(双引擎/冻结+异常检测/低频自检/自动切回/Params 标志) |
| openpilot/sunnypilot/modeld_v2/gpu_backend/models/registry.py | 动态注册: 扫描 models/ 顶层+子目录, metadata 驱动 slices/input_shapes/模式, 目录名=Model 参数名 (原硬编码 3 profile 删除) |
| openpilot/sunnypilot/modeld_v2/gpu_backend/build_plans.py | 新增: 扫描注册表, 缺 plan 且含 ONNX 的自动 trtexec 构建; --dry-run |
| selfdrive/modeld/models/BigCombo/ | 新增: sp 实体拷贝 driving_supercombo_fp16.plan (1.76GB, md5 一致 919aee95…) + metadata 改名 driving_supercombo_metadata.pkl |

## 四、Failover 策略(与车主确认)

- 主=BigCombo, 备=Classic(默认组合; Params Model 为空 → 仅 Classic 单模型)
- 备模型平时每 15 帧自检一次(维持时序状态, 不发布)
- 主触发条件: 推理异常 / 输出非有限 / 连续 5 帧冻结(0902 冻结案就是"不抛异常的坏", 官方 try/except 抓不到)
- 切备后: 备每帧推理发布, 主每 5 帧降频复测; 主连续 50 帧健康自动切回
- 单源: FailoverModelState.run() 一帧只返回一个模型的输出, modeld 是唯一发布者
- Params: Model=主, FallbackModel=备(空则自动: 大模型主跑 → Classic 备), FallbackActive=1/0(切换标志)

## 五、验证结果(2026-09-16 全部本地通过)

1. registry 动态注册: 11 个 profile(Classic/BigCombo ready, 0-8 未 ready 无 onnx)
2. BigCombo plan 实载: 输入 img/big_img (1,12,128,256) + desire_pulse/traffic_convention/
   action_t/features_buffer(1,24,512), 输出 (1,2580) fp16 —— 与 sp modeld_bigcombo 完全一致
3. FailoverModelState 双引擎实载: primary=BigCombo(merged), fallback=Classic(split) 同时驻留
4. 状态机 6 项测试全过(备自检/冻结切备/备每帧推/自动切回 248 帧/异常切备/prepare_only)
5. build_plans --dry-run: BigCombo/Classic ok, 0-8 manual

## 六、坑与注意

- tinygrad+TRT 混合进程退出时可能 core dump(清理顺序), 运行期无影响, 不影响 manager 拉起
- sp 的 FiletOFish fp16 plan 用 cp 的 trt_c_api 加载会段错误(版本差异) → 跨树 plan 拷贝不可靠,
  一律"拷 ONNX 本地重建"; BigCombo plan 实测可加载
- 0-8 目录只有 pkl 权重套件无 ONNX(TRT 无法建引擎, 保持 tinygrad 回退可用);
  要 TRT 化需从社区搞对应 ONNX
- cp 模型目录被 git 忽略(源/模型/产物), BigCombo 1.76GB 不入库
- 官方分支对照: 官方仅异常触发+不恢复+modelV2.big 字段; cp 版增强冻结/非有限检测+自动恢复+
  Params FallbackActive 标志

## 七、待办

- [ ] 实车/回放验证: MODEL_NAME=BigCombo 启动 modeld 观察 UI 与日志
- [ ] 0-8 社区模型 TRT 化(可选)
- [ ] FiletOFish 模型目录移植(拷 ONNX 三件套 + build_plans.py 重建)