# cpv9-dev 维护说明

本仓库已完成双远程配置：
- `origin`  = https://github.com/feiyang2025/cpv9-cuda.git  （自己的 GitHub，日常推送）
- `upstream` = https://jihulab.com/fishop/openpilot.git       （上游，拉取更新）

当前本地分支 `cpv9-dev` 跟踪 `origin/main`，三者已在提交 `45df2ae` 对齐。

---

## 一、拉取上游更新（jihulab 有新版本时）

在 `E:\Model\增加导航\fishop-openpilot-cpv9-dev` 目录执行：

```powershell
git fetch upstream                 # 拉取 jihulab 最新
git checkout main                  # 先切到自己的 main（如果没有 main 就 checkout -b main）
git merge upstream/cpv9-dev        # merge 合并上游更新（保留自己的改动）
# 如果有冲突：手动解决 → git add . → git commit -m "merge upstream"
git push origin main               # 推回自己的 GitHub
```

## 二、日常提交自己的改动

```powershell
git add .
git commit -m "改动说明"
git push origin main
```

## 三、查看当前状态

```powershell
git remote -v        # 查看两个远程
git branch -vv       # 查看分支跟踪
git log --oneline -10
```

## 四、注意事项

- 模型目录下有大于 50MB 的 `*.pkl` 文件，GitHub 只警告、不影响推送。
- 平台层（jetson-cuda-overlay）是独立仓库/工作区，不参与本仓库版本管理。
- 设备侧产物（`trt_c_api.so`、`.plan`、`libcuda_transform.so`）不要提交到 git。
