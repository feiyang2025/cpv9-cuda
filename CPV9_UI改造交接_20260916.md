# cpv9-cuda-main UI 模式映射改造 — 交接文档

> 生成:2026-09-16(原窗口运行约 17h 后交接)
> 目标树:/data/openpilot/cpv9-cuda-main(非 git 仓库,ajouatom/fishop cpv9 系拷贝树)
> 参照:jetson-cuda-overlay(overlay,git remote 已拉取合并到 09a7753)

## 一、背景与已完成工作(截止交接时)

### 1. cpv9 全链路适配已完成(可启动)
- 推理链路:CUDA/TensorRT(GpuModelState + Classic split 引擎实测推理通过)
- CH347 IMU:hidraw 新版 ch347t,实测数据流正常(104Hz,ACC/GYRO 正常)
- Panda:四分支统一 v17/57B/签名 1eddee73,不重刷固件
- 相机:tw_camera_cfg 生命周期在 launch 脚本管理;camerad 已编
- doctor.py 体检:全部通过 ✅
- **UI 改造(未编译验证完毕,见 §三)**

### 2. 启动问题已修
- build.json 生成(非 git 树需要)→ manager 不再报 invalid build metadata
- launch_chffrplus.sh:manager 退出后 exit(不再 while true 死循环)
- launch_openpilot.sh:去掉 `bash 1.sh` 噪音(原版拉远程脚本)
- DoShutdown 残留参数已清(用户点了关机按钮产生,正常行为)
- _text 已编(touch .lfsconfig 后 extras 生效)

### 3. 丰田标定(sp-cp-toyota-transfer)
- 工具在 /home/nvidia/data/openpilot/sp-cp-toyota-transfer/
- LEXUS_ES_TSS2 sp 模式已写入:**~/.comma/params/d 和 /data/params/d 两侧**
  - LateralTorqueCustom=0(自学习),AccelFactor=2036,Friction=102,NNFF=1
- 验证:12 键在 params_keys.h ✓;LEXUS_ES_TSS2 数值 cpv9/SP/presets 三处一致 ✓

## 二、当前进行中的工作:UI 控件中文映射改造

### 目标
把 settings.cc 里 CValueControl 的 0/1/2 裸数字显示改成**中文模式名/开关**,类似 SP 的开关体验。用户希望**逐步、分批**改造(每批人工确认语义),不要一次全改。

### 已实现的机制(CValueControl 改造)
- settings.h:`CValueControl` 构造加第 7 参 `QStringList map = {}`
- settings.cc:
  - `refresh()`:map 非空时按值显示中文(label 用 m_map.at(v))
  - `adjustValue()`:map 非空时**循环切换**(min..max 间取模),+/- 按钮变模式切换
- 已验证编译通过(第一批改完后 scons selfdrive/ui 成功)

### 已加映射的控件(14 处)
| 行号 | 参数 | 映射 |
|---|---|---|
| 750 | LateralTorqueCustom | {"自学习","自定义","默认值"} |
| 794 | ShowDebugUI | {"关","开","详细"} ⚠️ 见 §三 |
| 795 | ShowTpms | {"关","开","简略","详细"} ⚠️ 见 §三 |
| 796 | ShowDateTime | {"关","时间/日期","仅时间","仅日期"} |
| 797 | ShowPathEnd | {"关","开"} |
| 798 | ShowDeviceState | {"关","开"} |
| 800 | ShowRadarInfo | {"关","显示","相对位置","静止车辆"} |
| 801 | ShowRouteInfo | {"关","开"} |
| 901 | NNFF | {"关","开"} |
| 902 | NNFFLite | {"关","开"} |
| 940 | SameSpiCamFilter | {"关","开"} |
| 941 | StockBlinkerCtrl | {"关","开"} |
| 942 | ExtBlinkerCtrlTest | {"关","开"} |
| 943 | BlinkerMode | {"自动","仅变道"} |

## 三、遗留问题(新窗口先处理)

### ⚠️ 1. ShowDebugUI / ShowTpms 映射语义存疑(用户指出)
- 两者 desc 原本为空,映射是我**推断**的,未经代码验证
- 已查证部分语义:
  - ShowDebugUI:carrot.cc 用 `>1` 显示 carrot_man_debug(详细调试文本),`==0` 隐藏
    → 推测 0=关,1=常规,2=详细 — **映射顺序可能不对,需确认**
  - ShowTpms:carrot.cc 用 switch:0=不显示,1=drawTpms2,2=drawTpms3,3=两者
    → 我的 {"关","开","简略","详细"} 是**错的**:1 和 2 是两种胎压样式,3=都显示
    → 正确映射应类似 {"关","样式1","样式2","样式1+2"},需看 drawTpms2/3 实际含义
- **建议:新窗口先查 drawTpms2/drawTpms3 和 ShowDebugUI 的完整用法,修正这两处**

### 2. 剩余 180+ 个 CValueControl 未映射
- 0/1 类(约 20+ 处): 等
- 0/2、0/3 类:CruiseButtonMode/TrafficLightDetectMode/CanfdHDA2/RecordRoadCam/HDPuse/MapboxStyle/EnableConnect/AutoNaviSpeedCtrlMode/AutoNaviCountDownMode/TurnSpeedControlMode/AutoTurnControl 等
- **原则:每次改一批(如一个 toggles 分组),先 grep 参数在代码里的真实语义,再写映射;语义不明的保持数字显示**

### 2.5 ✅ 2026-09-16 下午已完成的批量(commit 07b5619~396bf92 全推送 GitHub)
- **git 化完成**:cpv9-cuda-main 已纳入 git(SSH 推送 github.com/feiyang2025/cpv9-cuda),每批改动先 commit 再改,可随时回滚
- **交互升级**:CValueControl 枚举控件 = 整行点击弹出全屏大按钮选择框(±循环保留),label 带 ▾ 提示;支持 -1/1 起始枚举
- **已映射 ~50 处**:首批 14(ShowTpms 修正/ShowDebugUI 等)+ 16 个 0/1 开关 + 16 个多模式控件 + SmoothStopMode(modelid 模型选择/MyDrivingMode/CruiseButtonMode/MapboxStyle/AutoNaviSpeedCtrlMode/TurnSpeedControlMode 等)
- **语义依据**:/home/nvidia/data/openpilot/CP功能设置说明.md(权威,五组参数详解)+ 代码交叉验证
- **分组标题机制**:makeSectionHeader() 辅助函数 + 18 个分区(巡航4/调节6/启动5/速度4/导航5/显示2),长列表全部分块
- **真模型切换**:新控件 CStringEnumControl(字符串枚举)+ "推理模型(GPU)"入口,写 Model 参数(CUDA/TRT 真实生效),modelid 保留仅影响 tinygrad 兜底
- **车型标定页(新)**:顶部第8个"标定"按钮 → 268 车型预设选择(读 selfdrive/ui/toyota_presets.json)+ SP/精确/恢复默认 三模式一键写入 12 个标定参数
- **保持数字**:SpeedFromPCM/EnableConnect/EnableRadarTracks/ShowDebugLog(语义不明或非枚举)

### 3. UI 未重新编译验证(14 处映射改完后)
- 最后一次 scons selfdrive/ui 是在第一批(LateralTorqueCustom)改完时验证通过的
- 之后又改了 NNFF/显示组/导航组,需要再跑 `scons -j8 selfdrive/ui` 验证

## 四、环境速查

```bash
# 编译 UI
cd /data/openpilot/cpv9-cuda-main && source .venv/bin/activate && scons -j8 selfdrive/ui
# 全量(会撞 panda 固件 arm-none-eabi 缺失—已知,prebuilt 已跳过,固件用统一版)
scons -j8
# 启动
bash launch_chffrplus.sh  # 或 ENABLE_FAKE_PANDA=1
# 体检
python3 /data/openpilot/jetson-cuda-overlay/doctor.py --short
# panda 自检
bash /data/openpilot/panda_版本核对/panda_boot_check.sh
```

关键产物(都在 cpv9-cuda-main 树内):
- libcuda_transform.so:已重编(UV Tin@P@Tout + temporal 修复版)
- ch347t:已重编(hidraw + json11 校准合并版)
- ui:21MB(编译中)
- .plan 引擎:driving_vision.plan + driving_policy.plan(复用 ajouatom)
- build.json:非 git 树 build metadata

## 五、踩坑记录(避免重复)

1. **scons 全量撞 panda 固件**:arm-none-eabi-gcc 已装,但 panda 固件源码(v16)与统一协议(v17)冲突,main_comms.h 已修(spi_error_count_pkt/fan_stall_count 移除)。**注意:panda_维护.sh 会以"当前主分支固件"为基准同步,若某分支重编了固件,基准会被带偏 — 恢复用固件备份目录 /data/openpilot/panda_版本核对/固件备份/ 里的 cuda_DEV-ccb9581b_HEALTHv17_1eddee73.bin.signed**
2. **build.json**:非 git 树必须,否则 manager 报 invalid build metadata
3. **future_fstrings**:acados_template 10 个文件删了 `# -*- coding: future_fstrings -*-`(Py3.12 报错)
4. **icu66shim.so**:QMapLibre 需 ICU66 符号,系统只有 74,shim 转发;源码在 jetson-cuda-overlay/third_party/icu66shim/,已装 /usr/local/lib,ui/mapsd 链接加 -licu66shim(必须在 -lQMapLibre 之后)
5. **OmxCore 缺失**:ui SConscript 已加 has_omxcore() 检测,无库时移除 screenrecorder(与 WSL2 分支等效)
6. **ffmpeg 4→6 迁移**:omx_encoder.h/cc 改了 av_register_all 移除、AVCodec→const、av_free_packet→av_packet_unref(虽然后来 screenrecorder 被移除了,改动保留无害)
7. **.lfsconfig**:cpv9 缺(ajouatom 有),导致 extras=False,_text 没编;已 touch 补上
8. **overlay 合并**:jetson-cuda-overlay 已 merge origin/main(74f527b UV/temporal 修复 + 36e8482 ch347t 校准),本地工作区保留 cpv9 验证过的改动;cuda_transform.py 是合并版(ctx 恢复 + register 修复)

## 六、下一步建议(新窗口)

1. 修 ShowTpms/ShowDebugUI 映射语义(查 drawTpms2/3 + ShowDebugUI 用法)
2. scons selfdrive/ui 编译验证全部映射
3. 按组逐步加映射(巡航组→横向组→启动组→速度组→导航组),每组先查语义
4. 相机/标定按 sp 对齐(之前提过:sp 的标定参数 FcamIntrinsics=961.76/EcamIntrinsics=1740 @1920x1080 在 ~/.commaspold/params/d,cpv9 camera.py 标定入口 patch 过 — 需复核)
5. 实车测试
