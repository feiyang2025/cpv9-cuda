# CPV9 模型"下载即用"方案设计(研究稿)

日期: 2026-09-16
状态: 方案设计, 未实施(待后续研究推进)
作者: 小马 + 用户讨论产出

## 一、问题

官方(commaai/openpilot/sunnypilot)"下载模型就能用", 而 cp 每次拿到新模型要手动跑
build_plans.py 编译 TRT engine, 体验差距大。用户诉求: 点击下载 → 模型自动可用,
"下载"本身变成一个处理过程。

## 二、根因(已核实)

| | 官方 | cp |
|---|---|---|
| 推理后端 | tinygrad 运行时 JIT | TensorRT 预构建 plan |
| 模型文件 | onnx/pkl(设备无关权重) | 同左(onnx 自带权重) |
| 引擎形态 | 首次推理时隐式编译, 用户无感 | plan 绑定 GPU 架构(sm_87), 必须显式构建 |
| 大模型 | precompiled-big-model 分支: 预编译分发 | 同机 sp→cp plan 可复用(实测 BigCombo md5 一致可加载) |

结论: 官方并没有"下载时编译"。普通模型靠 tinygrad 运行时 JIT(编译藏进推理第一帧),
大模型靠预编译分发(硬件固定, 引擎随 OTA 走)。官方两次都绕开了"用户显式编译"。

cp 走 TRT 路线(更快的 fp16 引擎), 代价是 plan 设备相关。但同配置 Orin 之间 plan 可复用
(已验证), 所以可以用"预编译分发"思路抹平体验差。

## 三、方案形态

### 模型包规范(核心)

每个模型一个自包含包(建议 tar.gz):

```
<ModelName>.tar.gz
├── driving_vision.onnx          # 权重(必带)
├── driving_policy.onnx          # 权重(必带)
├── driving_vision_metadata.pkl  # 切片/输入形状(必带)
├── driving_policy_metadata.pkl
├── driving_vision_fp16.plan     # 预编译引擎(推荐带, 同配置 Orin 通用)
├── driving_policy_fp16.plan
├── driving_vision_tinygrad.pkl  # 可选, tinygrad 回退用
└── driving_policy_tinygrad.pkl
```

merged 模型(BigCombo 类):
```
BigCombo.tar.gz
├── driving_supercombo_metadata.pkl
└── driving_supercombo_fp16.plan   # 1.7G, 包会很大; 可选不带 plan 现场编
```

plan 只需在任意一台同配置 AGX Orin 上构建一次, 永久随包分发。

### 安装动作(把"下载"变成处理过程)

用户侧输入 = 一个包(本地文件 / U盘 / 目录 / 未来的下载源):

1. 解包到 `selfdrive/modeld/models/<ModelName>/`(registry 按目录自动识别, 已有)
2. 校验: metadata + onnx + (plan 或可构建)
3. 有 plan → 直接完成, 上模型列表
4. 缺 plan 且只有 onnx → 自动触发 build_plans.py(日志提示"首次构建约 N 分钟")
5. 完成 → registry 刷新 → UI 模型列表可见 → 选择即用

### 入口设计(二选一或都做)

- A. 命令行: `python openpilot/sunnypilot/modeld_v2/gpu_backend/install_model.py <pack.tar.gz|dir>`
  (serif 简单, 脚本内调 build_plans 兜底)
- B. UI 入口: 模型设置里加"安装模型"按钮(读本地包), 走同一处理函数

### 兜底构建

install_model.py 内嵌调用 build_plans.py 的逻辑(缺 plan 时), 不重复实现。

## 四、与官方机制对照

- 官方 git-xet: 仓库内 134 字节指针 → 按需拉真实 onnx。cp 不需要 xet, 实体文件分发即可(模型目录已被 gitignore, 不入库)
- 官方 tinygrad JIT: cp 不学(放弃 TRT 优势不值得)
- 官方 precompiled 大模型: cp 学这个思路 → plan 随包分发

## 五、分期计划

- [ ] P1 研究: install_model.py 骨架(解包+校验+registry 刷新), 手工做一个 CD210 包验证
- [ ] P2 研究: 兜底构建路径(缺 plan 自动 trtexec), 测 BigCombo 大包(1.7G plan 打包/解包时间)
- [ ] P3 决策: UI 入口 vs 纯命令行; 模型来源(U盘/目录/后续下载源/模型池 models_pool 统一管理)
- [ ] P4 落地: 选定入口接入启动项/UI

## 六、决策点(下次继续时确认)

1. 模型包来源: 本地目录/U盘 先够用? 还是要预留远程下载源?
2. UI 入口 vs 命令行先行?
3. 大模型包(1.7G plan)是否随包分发, 还是默认现场编(省打包空间)?
4. 是否建统一模型池目录 /data/openpilot/models_pool/ 收所有 fork 的模型包?